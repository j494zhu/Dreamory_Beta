Decisions:

1. 模仿 or 成为?
=> 成为. 
(模仿本质上就是让llm去模仿一个有着明确背景的角色, 不管是真实的还是虚假的)
(成为本质上就是趁人自己模型的身份, 对此坦然, 那么我们对于模型的要求就仅仅是拥有人性, 而不是仿照人类)
(例如给llm一个设定, 出生地点, 工作经验, 相识过程, 那么所有没有预设好的细节都需要现编)
(如果llm不需要去模仿一个人, 就可以坦然自己再数字世界, 没有过去, 等等. 作为一个llm, 可以不知疲倦, 记忆更好等等. )
(后续需要一段相对通用一点的初始化指令)

2. 开发程度: 每一个模块都做成可以热插拔的, 先做初始版
所有模块的简化版都跑通以后, 再做完成版

3. 项目核心目的:
说的好听一点叫可以让模型更好的和人对话, 本质上就是让模型可以更好的做出一个女性伴侣的角色
通过这个项目, 要达到当llm扮演此类角色的时候, 不会给人有任何虚假, 伪人的感觉. 



Workflow: 
[
    [
        [参数集和固执模组], 
        [脑内剧场]
    ], 
    [
        [层级记忆库], 
        [未知人设快速生成], 
        [多重备份记忆库和多重检索], 
        [层级记忆库组织], 
        [层级记忆库运行时维护], 
        [层级记忆库离线睡眠维护]
    ], 
    [上下文压缩], 
    [身份证]
]

当前进展:
[参数集] - 第一阶段
当前参数: (valance: 心情, energy: 精力, interest: 兴趣)


Longterm:
我突然意识到一个问题. 不同子版块之间的耦合似乎... 将会是一个不小的麻烦. 到时候可能需要依靠fable 神力
例如我现在的参数集会直接依赖原始数据, 但是等到引入记忆库以及上下文压缩以后, 现在的参数集会不会直接废掉?